/**
 * Metadata class.
 * Holds metadata which goes into the head a document.
 */
export default class Metadata {
  /**
   * @constructor
   *
   * Metadata class.
   * Holds metadata which goes into the head a document.
   *
   * @example
   * ```
   * const metadata = new Metadata();
   * ```
   */
  constructor() {}
}
